import * as React from 'react';
import RateCalculator from './components/RateCalculator';

const App: React.FC = () => {
  return (
    <div>
      <RateCalculator />
    </div>
  );
};

export default App;
